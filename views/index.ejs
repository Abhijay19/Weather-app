<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link href="styles/main.css" rel="stylesheet"/>
    <title>Document</title>
</head>
<body>
  <div class=" mainbox border-bottom">
    <div class="bg-dark text-secondary px-4 pt-5 text-center">
      <div class="py-5">
        <h1 class="display-5 fw-bold text-white">Weather App</h1>
        <div class="col-lg-6 mx-auto">
          <p class="fs-5 mb-5 pb-2">Quickly get weather information of any city in the world</p>
        </div>
        <div>
    
          <form class = "list-group-item d-flex gap-3 justify-content-center" id="navBarSearchForm" action="/submit" method="POST">
            <input type="text"  class="form-control form-control-dark" name="cName" placeholder="city name" />
            <button type="submit" class="btn btn-outline-warning" value="OK" > OK </button>
          </form>
        </div>
        <div>
          <% if(locals.content){ %>
            <h1 class="main mt-5"><%= content.name %></h1>
            <%- include("partials/picture.ejs") %>
            <h2 class="fs-1 fw-bold main" ><%= Math.round(content.main.temp)  %>° C</h2>
            <h2><%= content.weather[0].description %></h2>
            <h2>Feels like <%=  Math.round(content.main.feels_like) %>° C</h2>
            <div class="d-inline-flex p-2 box mt-4 ">
              <div class="px-2 mx-5"> 
                  <img src="../images/images/humidity.png" width="60px" height="60px" class="mb-3">
                  <div class="fs-5">
                    <p class="mb-0 main fs-3"><%=  Math.round(content.main.humidity) %> % </p>
                    <p>Humidity</p>
                  </div>
              </div>
              <div class="mx-5">
                  <img src="../images/images/wind.png" width="60px" height="60px" class="mb-3">
                  <div class="fs-5">
                      <p class="mb-0 main fs-3"><%=  (content.wind.speed*3.6).toFixed(2)%> km/h</p>
                      <p>Wind Speed</p>
                  </div>
              </div>
            </div>
            
            <% } %>
            <% if(locals.contentErr){ %>
            <h2 class="mt-5"><%= contentErr.message %></h2>
            <% } %>
        </div>
      </div>
    </div>
  </div>
  <section class="bg-dark">
    <footer class="py-3">
    
      <p class="text-center text-secondary">© Abhijay Sekhar Choudhury,    All rights reserved </p>
    </footer>
  </section>
  
      
</body>

</html>
